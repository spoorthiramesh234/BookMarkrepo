function Navbar() {
  return (
    <nav style={styles.nav}>
      <h2>Smart Bookmark Manager</h2>
    </nav>
  );
}

const styles = {
  nav: {
    textAlign: "center",
    marginBottom: "20px"
  }
};

export default Navbar;